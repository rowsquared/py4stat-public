# config

Contains project configuration files.
- `catalog.yml`: dataset registry with paths, formats, and metadata.
- `settings.yml`: processing parameters and analysis settings.

In `catalog.yml`, supported formats are: `csv`, `parquet`, `xlsx`/`excel`, `dta`/`stata`, and `json`.
